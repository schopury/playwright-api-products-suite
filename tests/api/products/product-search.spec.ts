/*import { test, expect, PRODUCT_DATA } from '../../fixtures';
import { expectOkJsonResponse } from '../../../api-assertions';

const searchScenarios = PRODUCT_DATA.SEARCH_SCENARIOS;

test.describe('@api Products | SEARCH /products/search', () => {
  for (const tc of searchScenarios) {
    test(`search: returns results for ${tc.desc}`, async ({ products }) => {
      test.skip(true, 'TODO: implement search tests');
      //   const res = await products.searchProducts({ q: tc.query });
    });
  }
});*/
